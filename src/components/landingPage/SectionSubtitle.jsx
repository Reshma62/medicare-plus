const SectionSubtitle = ({ children }) => {
  return (
    <div className="max-w-[30em] w-full mx-auto mt-[.5em]">
      <p className="text-center text-[.95em] text-gray-500">{children}</p>{" "}
    </div>
  );
};

export default SectionSubtitle;
